using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrateCatcher : MonoBehaviour {

    // String to hold the product type this catcher is meant to grab, circle, square, triangle or trash.
    public bool isCircleCatcher = false;
    public bool isSquareCatcher = false;
    public bool isTriangleCatcher = false;
    public bool isTrashCatcher = false;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
